import 'package:flutter/material.dart';

class PageSouhait extends StatelessWidget {
  const PageSouhait({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return  Scaffold(
      body: Center(
        child: Container(
          child: Text("La page est en chantier"),
        ),
      ),
    );
  }
}
